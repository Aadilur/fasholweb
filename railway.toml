[build]
builder = "nixpacks"
buildCommand = "npm run build"

[deploy]
# 350MB heap + ~80MB V8/native overhead stays safely under the 500MB container limit.
startCommand = "NODE_OPTIONS='--max-old-space-size=350' npm run start"
numReplicas = 1
